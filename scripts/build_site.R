# Det er denne der faktisk renderer siden
babelquarto::render_website()

# Den installeres ved
# install.packages('babelquarto', repos = c('https://ropensci.r-universe.dev', 'https://cloud.r-project.org'))


# Skal vi se resultat:
# servr::httw("docs")